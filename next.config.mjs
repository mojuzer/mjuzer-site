/** @type {import('next').NextConfig} */
const nextConfig = {
  // React strict mode surfaces side-effect bugs early. Safe for a static marketing site.
  reactStrictMode: true,
};

export default nextConfig;
